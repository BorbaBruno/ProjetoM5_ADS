import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Projeto-Suporte',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
